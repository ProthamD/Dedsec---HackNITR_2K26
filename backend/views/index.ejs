<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventree - AI Inventory Manager</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-indigo-900 via-purple-900 to-pink-800 flex items-center justify-center p-6">
    <div class="w-full max-w-5xl">
        <!-- Header -->
        <div class="text-center mb-6 text-white animate-fade-in">
            <h1 class="text-6xl font-extrabold mb-3 drop-shadow-2xl tracking-tight">
                🤖 Inventree AI
            </h1>
            <p class="text-xl opacity-90 font-light">Your Intelligent Inventory Assistant</p>
        </div>
        
        <% if (!hasInventory) { %>
            <!-- No Inventory State -->
            <div class="bg-white/95 backdrop-blur-lg rounded-3xl shadow-2xl p-16 text-center transform hover:scale-105 transition-transform duration-300">
                <div class="text-7xl mb-6">📦</div>
                <h2 class="text-4xl font-bold text-gray-800 mb-4">No Inventory Data Found</h2>
                <p class="text-gray-600 text-lg mb-8">Please add inventory items to start using the AI assistant.</p>
                <a href="/inventory/add" class="inline-block bg-gradient-to-r from-purple-600 to-blue-500 text-white px-10 py-4 rounded-full font-semibold hover:shadow-2xl transition-all duration-200 hover:scale-110">
                    Add Inventory
                </a>
            </div>
        <% } else { %>
            <!-- Chat Interface -->
            <div class="bg-white/95 backdrop-blur-lg rounded-3xl shadow-2xl overflow-hidden border border-white/20">
                <!-- Inventory Info Bar -->
                <div class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 text-white px-8 py-4 flex justify-between items-center">
                    <div class="flex items-center gap-3">
                        <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                        <span class="font-semibold text-lg">📊 <%= inventoryCount %> Products</span>
                    </div>
                    <span class="text-sm opacity-90 bg-white/20 px-4 py-1 rounded-full">🆔 <%= userId %></span>
                </div>
                
                <!-- Chat Messages -->
                <div id="chatMessages" class="h-[550px] overflow-y-auto p-8 space-y-4 bg-black">
                    <!-- Bot Welcome Message -->
                    <div class="flex justify-start animate-slide-in">
                        <div class="bg-gradient-to-br from-indigo-500 via-purple-500 to-pink-500 text-white rounded-3xl rounded-tl-md px-7 py-5 max-w-[85%] shadow-xl">
                            <p class="leading-relaxed text-base">
                                👋 <strong>Hi there!</strong> I'm your Inventree AI assistant.<br><br>
                                I can help you with:<br>
                                <span class="inline-block mt-2">
                                    ✨ Inventory restocking decisions<br>
                                    📈 Demand forecasting & analytics<br>
                                    📊 Real-time stock analysis<br>
                                    💡 Cost optimization strategies
                                </span>
                                <br><br>
                                <em class="opacity-90">What would you like to know?</em>
                            </p>
                        </div>
                    </div>
                </div>
                
                <!-- Chat Input -->
                <div class="border-t border-gray-200 p-6 bg-white">
                    <div class="flex gap-4">
                        <input 
                            type="text" 
                            id="messageInput" 
                            placeholder="Ask me anything about your inventory..."
                            onkeypress="handleKeyPress(event)"
                            class="flex-1 px-7 py-4 border-2 border-gray-300 rounded-full focus:outline-none focus:ring-4 focus:ring-purple-300 focus:border-purple-500 transition-all duration-200 text-base placeholder-gray-400"
                        >
                        <button 
                            onclick="sendMessage()" 
                            id="sendBtn"
                            class="bg-gradient-to-r from-indigo-600 via-purple-600 to-pink-600 text-white px-10 py-4 rounded-full font-bold hover:shadow-2xl transition-all duration-200 hover:scale-110 active:scale-95"
                        >
                            <span class="flex items-center gap-2">
                                Send
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
                                    <path d="M10.894 2.553a1 1 0 00-1.788 0l-7 14a1 1 0 001.169 1.409l5-1.429A1 1 0 009 15.571V11a1 1 0 112 0v4.571a1 1 0 00.725.962l5 1.428a1 1 0 001.17-1.408l-7-14z" />
                                </svg>
                            </span>
                        </button>

                                        <a href="/inventory/add" class="inline-block bg-gradient-to-r from-purple-600 to-blue-500 text-white px-10 py-4 rounded-full font-semibold hover:shadow-2xl transition-all duration-200 hover:scale-110">
                    Add Inventory
                </a>
                </div>
            </div>
            
            <!-- Hidden input for userId -->
            <input type="hidden" id="userId" value="<%= userId %>">
        <% } %>


    </div>

    <script src="/js/chat.js"></script>
</body>
</html>
